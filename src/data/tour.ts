import type { TourStep } from '../types';

/** A guided walk that follows one UPDATE statement through the whole server. */
export const TOUR: TourStep[] = [
  {
    id: 'arrival',
    title: 'A statement arrives',
    district: 'thread-city',
    building: 'connection-gate',
    body: 'A client connects. MySQL hands the session its own thread and that thread will carry one statement all the way down. Nothing below Thread City understands SQL — everything past the executor is pages and rows.',
    set: { workload: 1 },
  },
  {
    id: 'plan',
    title: 'Parse, then choose a plan',
    district: 'thread-city',
    building: 'optimizer',
    body: 'The text is parsed into a tree, then the optimizer prices access paths using index statistics and cost constants. Bad plans are almost always stale statistics rather than a broken optimizer. The chosen plan becomes a stream of handler calls into InnoDB.',
  },
  {
    id: 'buffer',
    title: 'Everything happens in memory first',
    district: 'buffer-pool',
    building: 'page-grid',
    body: 'InnoDB reads the 16 KB page holding the row into the buffer pool and modifies it there. The disk copy is now stale. This is the central fact of InnoDB: your database is a memory system with a durable log bolted to the side.',
  },
  {
    id: 'lru',
    title: 'Why a big scan does not wreck the cache',
    district: 'buffer-pool',
    building: 'lru-old',
    body: 'New pages enter at the LRU midpoint, not the top, and must survive innodb_old_blocks_time before a second hit can promote them. A one-off full table scan therefore washes through the old sublist without evicting your hot working set.',
  },
  {
    id: 'redo',
    title: 'Write-ahead logging',
    district: 'redo-wharf',
    building: 'log-writer',
    body: 'Before that dirty page may ever reach disk, a redo record describing the change is appended to the log. Redo is small and sequential; pages are large and random. Committing means making the log durable, not the page.',
    set: { workload: 2 },
  },
  {
    id: 'durability',
    title: 'What "committed" costs',
    district: 'redo-wharf',
    building: 'commit-fsync',
    body: 'innodb_flush_log_at_trx_commit=1 fsyncs at every commit group — that is the ACID guarantee. Set it to 2 in the Control Room and watch the fsync beacon slow to once per second: faster, and up to a second of committed transactions lost on power failure.',
  },
  {
    id: 'binlog',
    title: 'The second log',
    district: 'replica-harbor',
    building: 'two-phase-commit',
    body: 'The commit also writes the binary log, in commit order, via internal two-phase commit: prepare in InnoDB, write the binlog, then commit. Redo makes this server crash-safe; the binlog makes replicas and point-in-time recovery possible.',
  },
  {
    id: 'replicas',
    title: 'Shipping the change',
    district: 'replica-harbor',
    building: 'applier-workers',
    body: 'A dump thread streams events to each replica, where a receiver thread stores them in the relay log and parallel workers apply them. Writeset dependency tracking lets independent transactions apply concurrently while commit order stays intact.',
  },
  {
    id: 'cleaners',
    title: 'The page still has to land',
    district: 'checkpoint-heights',
    building: 'page-cleaners',
    body: 'Page cleaner threads write the oldest dirty pages out continuously. This is fuzzy checkpointing — no stop-the-world pause, just a steady drip that lets the checkpoint LSN advance and frees redo space for reuse.',
    set: { workload: 3 },
  },
  {
    id: 'stall',
    title: 'When flushing falls behind',
    district: 'checkpoint-heights',
    building: 'sync-wall',
    body: 'If checkpoint age approaches redo capacity, InnoDB forces synchronous flushing and user threads simply wait — throughput sawtooths. Turn adaptive flushing off in the Control Room to see the ugly version, then turn it back on.',
  },
  {
    id: 'storage',
    title: 'Where the page lands',
    district: 'storage-yards',
    building: 'clustered-index',
    body: 'The write goes through the doublewrite buffer, then into the .ibd file — into the clustered index, the B+tree keyed by the primary key whose leaves hold the rows themselves. Random primary keys mean random writes and page splits.',
    set: { workload: 2 },
  },
  {
    id: 'mvcc',
    title: 'The old version is still alive',
    district: 'purge-gardens',
    building: 'read-view',
    body: 'The row you overwrote was not destroyed: its previous version sits in an undo log so any transaction with an older read view still sees it. Readers never block writers because of this — and it is why an idle open transaction is expensive.',
  },
  {
    id: 'purge',
    title: 'Collecting the garbage',
    district: 'purge-gardens',
    building: 'history-list',
    body: 'Purge threads remove dead versions and their index entries once no read view can need them. If history list length climbs steadily, something old is still open — find it in INNODB_TRX and kill it before undo eats the disk.',
    set: { workload: 1 },
  },
  {
    id: 'whole',
    title: 'The city, all at once',
    district: 'buffer-pool',
    body: 'That is the whole loop: parse and plan, read into memory, log the change, commit durably, ship it, flush lazily, and clean up old versions later. Every MySQL performance problem you will ever meet is one of these lanes backing up. Press G to explore freely.',
  },
];
